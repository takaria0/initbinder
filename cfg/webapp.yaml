paths:
  project_root: /Users/inagakit/Documents/UCIrvine/ChangLiu/Scripts/initbinder
cluster:
  ssh_config_alias: hpc3.rcic.uci.edu
  remote_root: /data/homezvol1/inagakit/Projects/initbinder    # adjust to your repo path on HPC
  target_root: /pub/inagakit/Projects/initbinder/  # adjust to your target root path on HPC
  control_path: /Users/inagakit/.ssh/cm-initbinder-hpc3.rcic.uci.edu  # adjust if needed
  control_persist: 600000
  conda_activate: "conda activate takashi"
  rsync_path: rsync
  sbatch_path: sbatch
  squeue_path: squeue
  sacct_path: sacct
  pymol_path: pymol
  assess_partition: standard          # optional, match your CPU queue
  assess_account: ccl_lab         # optional
  assess_time_minutes: 600
  assess_mem_gb: 4
  assess_cpus: 1
  debug: false
